apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-user
spec:
  selector:
    app: {{ .Release.Name }}-user
  ports:
    - port: {{ .Release.user.port }}
      targetPort: {{ .Release.user.port }}

